<IfModule mod_rewrite.c>
    RewriteEngine On
    # Bloquear acceso a carpetas sensibles y metadatos
    RewriteRule ^(\.git|\.agents|config|database|sessions) - [F,L]
</IfModule>

<FilesMatch "^\.env">
    Require all denied
</FilesMatch>

# Ocultar listado de directorios
Options -Indexes
